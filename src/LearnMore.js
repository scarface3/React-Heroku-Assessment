import React from "react";
import classes from "./LearnMore.module.css";

const LearMore = (props) => {
  return (
    <div className={classes.learnmore}>
      <button className={classes.learnmore_}>Learn More</button>
    </div>
  );
};

export default LearMore;
